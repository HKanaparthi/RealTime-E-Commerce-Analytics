# ShopStream Environment Variables
#
# Copy this file to .env and fill in your values

# Application
ENVIRONMENT=development
DEBUG=true

# API
API_HOST=0.0.0.0
API_PORT=8000

# Frontend
FRONTEND_URL=http://localhost:5173

# PostgreSQL Database
# Local: postgresql://postgres:postgres@localhost:5432/shopstream
# Neon (Free): Get from https://neon.tech dashboard (Settings > Connection String)
# Supabase (Free): Get from https://supabase.com project settings
# Railway: Provided by Railway PostgreSQL plugin
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shopstream

# Event Generator
# For free tier: Use lower rate (10-20) to reduce database load
# For paid tier: Can use higher rate (100-1000)
EVENT_RATE=10

# Optional: Kafka (for advanced setup only)
# For FREE TIER: Leave commented out - app will use simple event generator
# For PAID TIER: Uncomment to use Kafka + Spark streaming
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092
# SPARK_MASTER=local[*]
# SPARK_CHECKPOINT_DIR=/tmp/shopstream-checkpoints

# ==================================================================
# FREE TIER DEPLOYMENT SETUP (Render + Neon + Vercel)
# ==================================================================

# Backend (Render Free Tier)
# 1. Sign up at https://render.com
# 2. Create new Web Service from your GitHub repo
# 3. Set environment variables in Render dashboard:
#    - ENVIRONMENT=production
#    - DEBUG=false
#    - DATABASE_URL=<your-neon-connection-string>
#    - FRONTEND_URL=<your-vercel-frontend-url>
#    - EVENT_RATE=10
# 4. Build Command: pip install -r backend/requirements.txt
# 5. Start Command: cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT

# Database (Neon Free Tier)
# 1. Sign up at https://neon.tech
# 2. Create new project
# 3. Copy connection string from dashboard
# 4. Use it as DATABASE_URL above
# Example: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb

# Frontend (Vercel Free Tier)
# 1. Sign up at https://vercel.com
# 2. Import your GitHub repo
# 3. Set Root Directory: frontend
# 4. Set environment variable:
#    - VITE_API_URL=<your-render-backend-url>
# 5. Deploy

# ==================================================================
# PAID TIER DEPLOYMENT (Railway + Vercel) - Original Setup
# ==================================================================

# Railway Deployment (set these in Railway dashboard)
# DATABASE_URL=<from Railway PostgreSQL plugin>
# PORT=<automatically set by Railway>
# KAFKA_BOOTSTRAP_SERVERS=<your-kafka-service>

# Vercel Deployment (set in Vercel environment variables)
# VITE_API_URL=https://your-backend.railway.app
